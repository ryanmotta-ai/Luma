# LUMA — variáveis do relatório semanal (scripts/digest-semanal.py).
# Copie para .env (já está no .gitignore) para rodar local. Em produção, os
# mesmos nomes são secrets do GitHub Actions.
# ⛔ A service_role JAMAIS entra em arquivo versionado nem no front.

SUPABASE_URL=https://uqrqzjafhigjuvtjqzid.supabase.co
SUPABASE_SERVICE_ROLE_KEY=

# Conta que envia. SMTP_PASS é SENHA DE APP (Gmail/Workspace exigem), nunca a pessoal.
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=

# Opcionais
EMAIL_FROM=
EMAIL_CC=
EMAIL_SUBJECT=
LUMA_URL=https://ryanmotta-ai.github.io/Luma/
