/**
 * js/core/supabase-config.js
 *
 * Credenciais do projeto Supabase. PREENCHA com a Project URL e a anon key.
 *
 * A anon key é PÚBLICA por design — vai no front e está protegida pela RLS
 * (ver supabase/migrations/). NUNCA coloque aqui a chave `service_role`.
 *
 * Este arquivo É versionado de propósito: contém só chave pública (anon/
 * publishable), protegida pela RLS. A service_role JAMAIS entra aqui.
 */
window.LUMA_SUPABASE = {
  url:     'https://uqrqzjafhigjuvtjqzid.supabase.co',
  anonKey: 'sb_publishable_FijTD7XpddpIQVlc2oaz0Q_o7DglAeu',
};
