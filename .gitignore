# Dependências (não usamos, mas por precaução)
node_modules/

# macOS
.DS_Store
.AppleDouble
.LSOverride
._*

# Windows
Thumbs.db
ehthumbs.db
Desktop.ini

# Editores
.vscode/settings.json
.idea/
*.swp
*.swo
*~

# Logs
*.log

# Saídas de backup (geradas por scripts/backup-storage.js e pela CLI; nunca versionar)
backup/
storage-backup/
*.sql.gz

# Variáveis de ambiente (nunca commitar)
.env
.env.local

# NOTA: js/core/supabase-config.js é versionado DE PROPÓSITO — contém só a
# Project URL e a anon/publishable key (públicas, protegidas pela RLS).
# A chave service_role JAMAIS deve ser commitada (em nenhum arquivo).