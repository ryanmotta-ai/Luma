# Dependências (não usamos, mas por precaução)
node_modules/

# macOS
.DS_Store
.AppleDouble
.LSOverride
._*

# Windows
Thumbs.db
ehthumbs.db
Desktop.ini

# Editores
.vscode/settings.json
.idea/
*.swp
*.swo
*~

# Logs
*.log

# Saídas de backup (geradas por scripts/backup-storage.js e pela CLI; nunca versionar)
backup/
storage-backup/
*.sql.gz

# Variáveis de ambiente (nunca commitar)
.env
.env.local

# Config local do Supabase — as credenciais ficam DENTRO deste arquivo
# (que é ignorado), nunca no próprio .gitignore (que É versionado).
js/core/supabase-config.js